{
  "name": "xdrarport",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "echo 'Using committed manifest - skipping generation'",
    "build": "rm -rf dist && vite build",
    "preview": "vite preview",
    "generate-media": "node scripts/generate-media-manifest.js",
    "upload-videos": "node scripts/upload-to-r2.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.946.0",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.4.21"
  }
}
