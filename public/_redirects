# Cloudflare Pages redirects - Minimal required for subdirectory routing
# Redirect paths without trailing slashes to directories with trailing slashes
/clients/dna-lounge    /clients/dna-lounge/   200
